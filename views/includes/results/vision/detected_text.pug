h4 Detected text:

if data.vision
  for field in data.vision.detectText
    each value, key in field
      ul
        li #{key}: #{value}
  else
    ul
      li Sorry, no info!
else
    ul
      li Sorry, no info!
