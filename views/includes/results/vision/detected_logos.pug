h4 Detected Logos:

if data.vision
  for field in data.vision.detectLogos
    each value, key in field
      ul
        li #{key}: #{value}
  else
    ul
      li Sorry, no info!
else
    ul
      li Sorry, no info!
